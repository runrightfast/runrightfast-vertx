runrightfast {
    app {
        # The application group, name, and version are required
        # group = app-group-name
        # name = app-name
        # version = app-version
    }
    vertx {
        VertxOptions {
            metricsOptions {
                jmxDomain = ${runrightfast.jmx.default-domain}.metrics
            }
        }
    }
    jmx {
        default-domain = "co.runrightfast"            
    }
    logConfigOnStartup = true
}